;; org0.el Org stuff

(setq org-agenda-files (list org-directory))

;; (setq org-agenda-text-search-extra-files
;;       '(agenda-archives
;;         "path/to/files"))

(provide 'org0)
;; org0.el ends

